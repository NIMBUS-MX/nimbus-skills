# `.claude/CONTEXT.md` — project brief for the next Claude Code session

<!--
Maintained by the `context-brief` skill (nimbus-skills). To refresh:
run `/context-brief update` in this repo. Never edit by hand unless you
know what you're doing — the skill will overwrite this file on next update.

Never paste secrets. Reference paths only.
-->

## 0. TL;DR

One paragraph. What is this repo, what's the current focus of work, where are
things half-finished. The reader should be able to answer "should I touch this
right now?" from this section alone.

## 1. Repo inventory

For a single-repo project, this is one row. For a monorepo, one row per
top-level subproject.

| Path | Stack | Purpose | Default branch | Current branch | Dirty? |
|---|---|---|---|---|---|
| `./` | e.g. Python 3.11 / Django 4.2 | one-line role | `dev` | `feat/xyz` | ✓ 3 files |

## 2. Per-subproject brief

### `<subproject>`

- **Stack**: language, framework, version pins.
- **Build/deploy**: Docker/zip/plain, CI provider, deploy target.
- **Runtime targets**: preprod URL, prod URL, region, provider.
- **Load-bearing rules** (verbatim from CLAUDE.md — do not paraphrase):
  - "…"
  - "…"
- **Directory map** (top-level dirs, one line each):
  - `path/` — role.
- **Key entry points**:
  - `path/to/entry.py:42` — one-line role.
- **Backend integrations** (which services POST where, which read the DB directly).
- **Gotchas** — verbatim "will bite you" warnings.
- **Release / deploy commands** (verbatim):
  ```
  .scripts/release.sh patch
  ```
- **Test / lint commands** (verbatim):
  ```
  yarn lint && yarn format:check && npx tsc --noEmit
  ```
- **Uncommitted files** (if any):
  - `path/to/file` — one-line what it is.
- **In-flight branches** (if any):
  - `feat/xyz` — last commit "…".

## 3. Cross-repo / cross-subproject relationships

- Data flow: X → SQS → Y → HTTP → Z.
- Shared code paths worth knowing.

## 4. In-flight work

For each `*_PLAN.md`, `PROGRESS.md`, `[FERIA] *` doc, roadmap, ClickUp / Linear
ticket referenced anywhere:

- **`FOO_PLAN.md`** — one-line summary. **Status**: in-progress / blocked /
  drafting. **Next step**: … **Owner**: name. **Deadline**: YYYY-MM-DD.
- **ClickUp `wdnmuvc4wv`** — title. Status.
- **GitHub PR NIMBUS-MX/repo#28** — title. Status.

## 5. Recent activity

Last 10 commits per subproject with SHA + short message:

```
abc1234 fix: …
def5678 feat: …
```

## 6. Samples / drafts / iteration artifacts

If `samples/`, `drafts/`, or similar folders exist and are gitignored, list
files by role. Do not dump contents.

- `samples/reporte_cidcore_v11.pdf` — v11 iteration of the CIDCore report,
  wkhtmltopdf-rendered Letter Landscape, layout locked.
- `samples/render.sh` — reusable wkhtmltopdf docker render helper.

## 7. Operator notes / secrets pointers

If `nocommit/` exists:

- `nocommit/MIGRATE_SECRETS.md` — secrets migration plan. Do NOT paste
  contents. Read from that file directly if needed.

Never include secret values in this brief.

## 8. External deps a fresh machine needs

- Docker images: `docker pull surnet/alpine-wkhtmltopdf:3.20.3-0.12.6-full`.
- `gh auth login`.
- MCP servers: `claude mcp add …` list.
- Language toolchain versions (from `.tool-versions` or engines).
- Env files to recreate: which templates + where to source secrets from.

## 9. Where to start

- **First command**: `cd <subproject> && cat CLAUDE.md`.
- **Most active subproject/branch**: `<name>` — reason.
- **3-item punch list** of things mid-way:
  1. …
  2. …
  3. …

## 10. What NOT to touch

- Deprecated / vendored / auto-generated dirs.
- Legacy shims requiring coordination before change.

## 11. Appendix — verbatim CLAUDE.md excerpts

For each subproject's `CLAUDE.md`, quote the sections that are load-bearing —
releases, environments, deploy commands, security rules. Prose omitted; keep
only the operational.

<details>
<summary><code>./CLAUDE.md</code> — Releases section</summary>

```
verbatim excerpt here
```

</details>

---

<sub>Generated by `context-brief` skill (nimbus-skills) — YYYY-MM-DDTHH:MM:SSZ — repo SHA `abc1234` — v1.0.0</sub>
